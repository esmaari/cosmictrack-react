/**
 * @deprecated Import from `@/shared/content/tarot` instead.
 * Kept for backward compatibility during migration.
 */
export {
  tarotCards,
  tarotReadingCards,
  type TarotCard,
  type TarotReadingCard,
} from "@/shared/content/tarot/tarotReading"
